{
    "buildCommand": "turbo run build --filter=@bosdb/web...",
    "devCommand": "turbo run dev --filter=@bosdb/web",
    "installCommand": "npm install",
    "framework": "nextjs",
    "outputDirectory": "apps/web/.next"
}